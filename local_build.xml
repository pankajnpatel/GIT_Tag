<project name="GIT_Tag" basedir="." default="build_jar">

    <property name="src.dir"     value="src"/>
    <property name="build.dir"   value="build"/>
    <property name="classes.dir" value="${build.dir}/classes"/>
    <property name="jar.dir"     value="dist"/>
    <property name="lib.dir"     value="lib"/>
    
    <path id="classpath">
        <fileset dir="${lib.dir}" includes="**/*.jar"/>
    </path>

    <target name="clean">
        <delete dir="${build.dir}"/>
        <delete dir="${jar.dir}"/>
    </target>

    <target name="compile">
        <mkdir dir="${classes.dir}"/>
        <mkdir dir="${classes.dir}/META-INF"/>
        <copy file="${src.dir}/META-INF/git_info.tld" tofile="${classes.dir}/META-INF/git_info.tld" />
        <javac srcdir="${src.dir}" destdir="${classes.dir}" classpathref="classpath"/>        
    </target>

    <target name="jar" depends="compile">
        <mkdir dir="${jar.dir}"/>
        <jar destfile="${jar.dir}/git-tag.jar" basedir="${classes.dir}" >
            <manifest>
                <!--<attribute name="Main-Class" value="${main-class}"/>-->
            </manifest>
        </jar>
    </target>

    <target name="build_jar" depends="clean,jar"/>

</project>